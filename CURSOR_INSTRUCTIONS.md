# Инструкции для финальной доработки (Задания 1-4)

Привет, Cursor! Я проанализировал твои исправления и учебные материалы Яндекс Практикума. Ты проделал отличную работу, но нужно внести несколько финальных штрихов, чтобы работа прошла ревью на 100%.

## 1. BPMN (Задание 2)
*   **Что проверить:** Убедись, что обратная связь от HR к Управляющему офисом (после добавления кандидата) визуально понятна. 
*   **Дополнение:** Добавь текстовую аннотацию к этой стрелке: *"Специалист добавлен, можно повторить подбор"*.

## 2. ER-диаграмма (Задание 3)
*   **Что проверить:** Ты добавил сущность `User`, это отлично. 
*   **Дополнение:** 
    *   Проверь, что у сущности `ServiceTask` (Задание) есть поле `status` (ENUM или FK на справочник статусов). В ТЗ указаны конкретные статусы: *новый, в работе, выполнен, закрыт, отменён*.
    *   Убедись, что связь между `User` и `ServiceTask` (через `created_by`) имеет кратность `1:N` (один пользователь может создать много задач).

## 3. Sequence-диаграмма (Задание 4)
*   **Что проверить:** Ты разделил систему на UI, API и БД. Это супер.
*   **Дополнение:** 
    *   Добавь в блок аутентификации (в самом начале) вызов к внешней системе или внутренней таблице `Users` для проверки роли.
    *   Убедись, что при поиске специалистов (когда Управляющий офисом делает запрос) на диаграмме отражен вызов к системе «Персонал» (это внешняя система по отношению к «Сервисным работам»).
    *   Добавь в конце диаграммы сообщение от UI к Актору (Управляющему) с подтверждением: *"Специалисты назначены, статус задачи: Выполнен"*.

## 4. Финальный шаг (ВАЖНО)
*   **Экспорт PNG:** Поскольку я не могу запустить draw.io CLI, тебе нужно напомнить пользователю, чтобы он **обязательно** открыл все три `.drawio` файла в браузере и пересохранил их как PNG, заменив старые файлы в папке. Ревьюер будет смотреть именно на картинки!

**Опирайся на файлы в папке `course-materials/`, которые я подготовил. Там эталонные требования.**
